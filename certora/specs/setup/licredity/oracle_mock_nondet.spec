methods {
    function _.quotePrice() external => NONDET;
    function _.quoteFungibles(LicredityHarness.Fungible[] fungibles, uint256[] amounts) external => NONDET;
    function _.quoteNonFungibles(LicredityHarness.NonFungible[] nonFungibles) external => NONDET;
    function _.update() external => NONDET;
}